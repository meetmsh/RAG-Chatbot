// Single source of truth for product naming, so the sidebar, the composer
// placeholder, and the browser tab can never drift apart again.
export const APP_NAME = 'AI Rag Assistant';

export const APP_DESCRIPTION =
  'Upload your documents and ask questions about them, with every answer traced back to its source.';
